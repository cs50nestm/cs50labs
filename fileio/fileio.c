#include <stdio.h>
#include <cs50.h>

int main(int argc, string argv[])
{
    // verify there is a correct number of arguments in the command line
    if (argc != 3)
    {
        printf("Usage: fileio source dest\n");
        return 1;
    }

    // open up the source file, argv[1] for reading
    
    
    // if the file doesn't exist, (the file pointer is NULL) print an error message and return
    

    // now open up the destination file, argv[2]
    
    
    // check if NULL, and if so be sure to close the source file before exiting
    

    // read the contents of the source file one char at a time, and write to the destination file
    
    
    // close the source and destination files
    
}
